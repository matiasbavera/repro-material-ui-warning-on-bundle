import Typography from '@material-ui/core/Typography';
import React from 'react';

export const Problematic = (): JSX.Element => {
  return (
    <Typography>test</Typography>
  );
};

export default Problematic;